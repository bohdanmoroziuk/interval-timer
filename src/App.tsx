import { FC } from 'react';

import GlobalStyle from 'styles/global';

const App: FC = () => (
  <>
    <GlobalStyle />
    <div className="app">
      Interval Timer
    </div>
  </>
);

export default App;
